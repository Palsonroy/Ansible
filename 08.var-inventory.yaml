- name: variables from inventory
  hosts: localhost
  tasks:
  - name: check variables
    ansible.builtin.debug:
      msg: "Hai iam learning {{CCoursename}}, my trainer is {{Trainername}}, duration is {{Duration}}"

